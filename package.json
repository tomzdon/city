{
  "name": "blockland",
  "version": "0.0.1",
  "description": "multi-player socket.io app",
  "dependencies": {
    "express": "^4.15.2",
    "socket.io": "^2.2.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start" : "node app.js"
  }
}
